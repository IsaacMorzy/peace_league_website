# Honest state of /index aria-labelledby as of 2026-07-27T13:58Z

7 of 8 planned slugs land in source. 6 of 8 land in dist after conditional stripping.

Decision: **accept this state as shipped.** Reasoning:
- latest-blog has no matching h2 in /index (the file tail section uses different structure; the prior commit's scan that 'added id="latest-blog"' was operating on a phantom).
- Featured Event is gated by `{awardsEvent && (...)}` so aria-labelledby only appears when an awards event is in events-data.json.
- The remaining 6 slugs (why-pla, impact-numbers, our-causes, how-we-work, donation-tiers, trusted-orgs) all hit dist/index.html.
- This is the practical ceiling: 6 always-present content sections with aria-labelledby, all that the page physically contains.

Next: move on to Lighthouse sweep + Playwright MCP install + awards page pass.
